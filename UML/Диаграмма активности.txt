@startuml
title Activity Diagram ""

start
repeat

:Player нажимает кнопку;

fork
 :Player нажимает кнопку выстрела;



  fork 
      if (Значение счетчика "перезарядка" больше 1.5?) then(да)
          :Установить флаг выстрел в состояние "выстрел";
          :Присвоить снаряду значение кинетической энергии;
          :Показать анимацию выстрела;
          :Создать обьект снаряд;
          :Установить флаг выстрел в состояние "перезарядка";
          :Обнуление счетчика "перезарядка";
      endif

  end fork

fork again
  :Player нажимает кнопки движения;
  
  fork
    :Нажата кнопка вперед;
    :Обьект перемещается вперед со скоростью 4f вперед;

  fork again
    :Нажата кнопка назад;
    :Обьект перемещается назад со скоростью 4f вперед;

  fork again
    :Нажата кнопка поворота;
    :Обьект поворачивает со скоростью 50f;

  end fork

end fork

repeat while (Танк игрока уничтожен?)
stop
@enduml